import { profile } from './profile';
import { parts } from './parts';
import { admin } from './admin';
import { events } from './events';

export const server = {
  profile,
  parts,
  admin,
  events,
};
